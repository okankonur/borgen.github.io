---
layout: base.njk
title: Okan's
permalink: /index.html
---
<article>
  <h1>{{ site.title }}</h1>
  <p class="subtitle">{{ site.description }}</p>

  <ul class="post-list">
    {% for post in collections.posts %}
    <li>
      {% set en = collections.postsAll | translationOf(post.data.resolvedRef, post.data.resolvedLang) %}
      <span class="post-title">
        {% if en and en.data.resolvedLang == "en" %}<a class="lang-tag" href="{{ en.url }}" aria-label="Read in English" title="Read in English">EN</a>{% endif %}
        <a class="title-link title-default" href="{{ post.url }}">{{ post.data.title }}</a>
        {% if en and en.data.resolvedLang == "en" %}<a class="title-link title-alt" href="{{ en.url }}">{{ en.data.title }}</a>{% endif %}
      </span>
      <span class="post-list-date">{{ post.date | dateHuman }}</span>
      {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
    </li>
    {% endfor %}
  </ul>
</article>
